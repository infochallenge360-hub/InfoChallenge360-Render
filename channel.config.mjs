// ═══════════════════════════════════════════════════════════════
//  هوية القناة — مصدر الحقيقة الوحيد (single source of truth).
//  لإطلاق قناة جديدة: انسخ المشروع، وعدّل هذا الملف + التصاميم فقط
//  (الألوان/الماسكوت/الثمبنيل). كل السكربتات والـSEO تقرأ من هنا.
//  الباقي (المعايرة/المعلومات/الصوت/الريلز/QA) لا يتغيّر بين القنوات.
// ═══════════════════════════════════════════════════════════════
export const CHANNEL = {
  // — الهوية النصية (تتغيّر لكل قناة) —
  name: "Info Challenge 360",        // اسم القناة (يظهر في الوصف + العلامة)
  handle: "@InfoChallenge360",       // معرّف يوتيوب (رابط قابل للنقر)
  tagline: "outfox the clock.",
  brandWord: ["INFO CHALLENGE", "360"], // تقسيم العلامة المائية (الجزء2 بلون ذهبي)
  mascot: "owl explorer",            // بومة بزي مستكشف (توفيق بين اللمبة والبوصلة/الأهرام بالشعار)

  // — المسارات (تتغيّر لكل قناة) —
  drivePath: "D:/InfoChallenge360-Channel/02_Episodes",
  brandPath: "D:/InfoChallenge360-Channel/00_Brand",

  // — الألوان (تتغيّر لكل قناة — تُستخدم في GAME palette + الأصول) —
  colorPrimary: "#14315C",           // أزرق كحلي (مطابق للوقو)
  colorAccent: "#F2B705",            // ذهبي (مطابق للوقو)

  // — ثابت عبر القنوات (نادراً يتغيّر) —
  voice: "en-US-GuyNeural",          // صوت edge-tts المجاني
  introRate: "-6%",                  // سرعة نطق المقدّمة (أبطأ = أوضح)
};

// ملاحظة "التصاميم" (تتغيّر يدوياً لكل قناة — هذا ما يقصده المالك بـ"عدّل التصاميم"):
//  • ألوان الهوية: GAME palette في src/Quiz/quizv2.jsx (تُضبط على colorPrimary/colorAccent أعلاه)
//  • الماسكوت: public/brand/owl-think.png · owl-cheer.png (تُستبدل بصور الثعلب)
//  • العلامة المائية النصية: مكوّن Watermark في quizv2.jsx (يقرأ brandWord أعلاه)
//  • الثمبنيل: مكوّنات GsThumb* + أصول public/brand/
export default CHANNEL;
